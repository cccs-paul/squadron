spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://localhost/realms/squadron
          jwk-set-uri: https://localhost/realms/squadron/protocol/openid-connect/certs
