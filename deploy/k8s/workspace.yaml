apiVersion: v1
kind: ConfigMap
metadata:
  name: squadron-workspace-config
  namespace: squadron
  labels:
    app.kubernetes.io/name: workspace
    app.kubernetes.io/part-of: squadron
data:
  SPRING_PROFILES_ACTIVE: prod
  SERVER_PORT: "8086"
  SPRING_DATASOURCE_URL: jdbc:postgresql://squadron-postgresql:5432/squadron_workspace
  NATS_URL: nats://squadron-nats:4222
  WORKSPACE_PROVIDER: kubernetes
  WORKSPACE_K8S_NAMESPACE: squadron-workspaces
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: squadron-workspace
  namespace: squadron
  labels:
    app.kubernetes.io/name: workspace
    app.kubernetes.io/part-of: squadron
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: workspace
  template:
    metadata:
      labels:
        app.kubernetes.io/name: workspace
        app.kubernetes.io/part-of: squadron
    spec:
      serviceAccountName: squadron-workspace
      containers:
        - name: workspace
          image: squadron/workspace:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8086
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SERVER_PORT
              value: "8086"
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://squadron-postgresql:5432/squadron_workspace
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: squadron-postgresql-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: squadron-postgresql-credentials
                  key: password
            - name: NATS_URL
              value: nats://squadron-nats:4222
          envFrom:
            - configMapRef:
                name: squadron-workspace-config
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: squadron-workspace
  namespace: squadron
  labels:
    app.kubernetes.io/name: workspace
    app.kubernetes.io/part-of: squadron
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: workspace
