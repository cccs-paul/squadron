{{- if .Values.workspace.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: squadron-workspace-config
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "workspace") | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: prod
  SERVER_PORT: {{ .Values.workspace.port | quote }}
  SPRING_DATASOURCE_URL: {{ include "squadron.jdbcUrl" (dict "dbName" .Values.workspace.database.name) }}
  NATS_URL: nats://squadron-nats:4222
{{- end }}
