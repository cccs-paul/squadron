{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: squadron-config-config
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "config") | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: prod
  SERVER_PORT: {{ .Values.config.port | quote }}
  SPRING_DATASOURCE_URL: {{ include "squadron.jdbcUrl" (dict "dbName" .Values.config.database.name) }}
  NATS_URL: nats://squadron-nats:4222
{{- end }}
