{{- if .Values.notification.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: squadron-notification-config
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "notification") | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: prod
  SERVER_PORT: {{ .Values.notification.port | quote }}
  SPRING_DATASOURCE_URL: {{ include "squadron.jdbcUrl" (dict "dbName" .Values.notification.database.name) }}
  NATS_URL: nats://squadron-nats:4222
{{- end }}
