{{- if .Values.git.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: squadron-git
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "git") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.git.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron.selectorLabels" (dict "root" . "service" "git") | nindent 4 }}
{{- end }}
