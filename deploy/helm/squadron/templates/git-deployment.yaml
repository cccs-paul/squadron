{{- if .Values.git.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: squadron-git
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "git") | nindent 4 }}
spec:
  replicas: {{ .Values.git.replicas }}
  selector:
    matchLabels:
      {{- include "squadron.selectorLabels" (dict "root" . "service" "git") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "squadron.serviceLabels" (dict "root" . "service" "git") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      serviceAccountName: {{ include "squadron.serviceAccountName" (dict "service" "git") }}
      containers:
        - name: git
          image: {{ include "squadron.image" (dict "root" . "image" .Values.git.image) }}
          imagePullPolicy: {{ .Values.git.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.git.port }}
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SERVER_PORT
              value: {{ .Values.git.port | quote }}
            - name: SPRING_DATASOURCE_URL
              value: {{ include "squadron.jdbcUrl" (dict "dbName" .Values.git.database.name) }}
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: squadron-postgresql-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: squadron-postgresql-credentials
                  key: password
            - name: NATS_URL
              value: nats://squadron-nats:4222
            {{- range $key, $value := .Values.git.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: squadron-git-config
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.git.resources | nindent 12 }}
{{- end }}
