{{- if .Values.review.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: squadron-review
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "review") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.review.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron.selectorLabels" (dict "root" . "service" "review") | nindent 4 }}
{{- end }}
