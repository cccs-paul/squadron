{{- if .Values.workspace.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: squadron-workspace
  namespace: {{ include "squadron.namespace" . }}
  labels:
    {{- include "squadron.serviceLabels" (dict "root" . "service" "workspace") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.workspace.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron.selectorLabels" (dict "root" . "service" "workspace") | nindent 4 }}
{{- end }}
