{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "squadron-orchestrator.fullname" . }}
  labels:
    {{- include "squadron-orchestrator.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "squadron-orchestrator.selectorLabels" . | nindent 6 }}
{{- end }}
