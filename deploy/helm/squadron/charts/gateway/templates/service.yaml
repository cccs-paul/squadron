apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-gateway.fullname" . }}
  labels:
    {{- include "squadron-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-gateway.selectorLabels" . | nindent 4 }}
