apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-workspace.fullname" . }}
  labels:
    {{- include "squadron-workspace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-workspace.selectorLabels" . | nindent 4 }}
