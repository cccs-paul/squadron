apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "squadron-workspace.fullname" . }}
  labels:
    {{- include "squadron-workspace.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
