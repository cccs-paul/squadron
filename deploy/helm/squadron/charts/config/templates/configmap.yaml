apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "squadron-config.fullname" . }}
  labels:
    {{- include "squadron-config.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
