{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "squadron-review.fullname" . }}
  labels:
    {{- include "squadron-review.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "squadron-review.selectorLabels" . | nindent 6 }}
{{- end }}
