apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-review.fullname" . }}
  labels:
    {{- include "squadron-review.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-review.selectorLabels" . | nindent 4 }}
