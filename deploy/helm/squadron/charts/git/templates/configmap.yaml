apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "squadron-git.fullname" . }}
  labels:
    {{- include "squadron-git.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
