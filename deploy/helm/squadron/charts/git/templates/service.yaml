apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-git.fullname" . }}
  labels:
    {{- include "squadron-git.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-git.selectorLabels" . | nindent 4 }}
