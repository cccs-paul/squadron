apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "squadron-ui.fullname" . }}
  labels:
    {{- include "squadron-ui.labels" . | nindent 4 }}
data:
  default.conf: |
    {{- .Values.nginxConfig | nindent 4 }}
