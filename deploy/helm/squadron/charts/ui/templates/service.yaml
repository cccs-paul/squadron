apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-ui.fullname" . }}
  labels:
    {{- include "squadron-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-ui.selectorLabels" . | nindent 4 }}
