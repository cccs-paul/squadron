apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-notification.fullname" . }}
  labels:
    {{- include "squadron-notification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-notification.selectorLabels" . | nindent 4 }}
