{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "squadron-agent.fullname" . }}
  labels:
    {{- include "squadron-agent.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "squadron-agent.selectorLabels" . | nindent 6 }}
{{- end }}
