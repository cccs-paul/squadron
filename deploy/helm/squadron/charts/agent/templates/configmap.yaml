apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "squadron-agent.fullname" . }}
  labels:
    {{- include "squadron-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
