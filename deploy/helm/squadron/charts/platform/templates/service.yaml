apiVersion: v1
kind: Service
metadata:
  name: {{ include "squadron-platform.fullname" . }}
  labels:
    {{- include "squadron-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squadron-platform.selectorLabels" . | nindent 4 }}
