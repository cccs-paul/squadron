apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "squadron-identity.fullname" . }}
  labels:
    {{- include "squadron-identity.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
